# Codenotch for Windows — issue tracker

Status: FIXED = changed in this repo and unit-tested on Linux; needs a Windows run to confirm where noted.

| ID | Pri | Area | Problem | Evidence | Fix | Test | Status |
|---|---|---|---|---|---|---|---|
| W-01 | P0 | Install | An installer built from `tauri.conf.json` ships without `codenotch-hook.exe`. "Install Claude Code hooks" then fails with "missing codenotch-hook.exe", so Claude's working/waiting state never appears. | `hooks_install.rs:66-73`, no `externalBin` in `tauri.conf.json` | Hook shipped as Tauri sidecar via `tauri.release.conf.json`; CI + `scripts/build-installer.ps1` stage it | Install on Windows, run tray → Install hooks | FIXED — confirm on Windows |
| W-02 | P0 | CI | No CI runs in this repo: both workflows only run for `vinzdg/codenotch`. Windows code is never built or tested. | `.github/workflows/*.yml` `if: github.repository == 'vinzdg/codenotch'` | New `.github/workflows/windows.yml`: tests, installer artifact, tag release, secret scan | First run on GitHub | FIXED — confirm first run |
| W-03 | P0 | Security | Local event server accepted requests from any web page (no Origin/Host check). Any site could fake Working/Waiting states or add fake provider cells; a DNS-rebinding page could read `/activity`, which includes session names and prompt text. | `server.rs` (no header checks) | Refuse any `Origin` header and non-loopback `Host`; validate provider ids (`[a-z0-9_-]{1,32}`) | 4 unit tests in `server.rs` | FIXED |
| W-04 | P1 | Security | UI inserted provider id/name, notes, and labels into `innerHTML` unescaped, with CSP disabled and global Tauri API on. | `notch.html` render functions, `tauri.conf.json` `csp: null` | All dynamic text through `esc()` | JS syntax check only | FIXED (escaping). CSP still open → W-18 |
| W-05 | P1 | Reliability | Pushed activity rows unbounded (30 min TTL each). | `activity.rs` `PUSHED` map | Cap at 64 rows; expired rows evicted first | None (shared global makes a safe test awkward) | FIXED |
| W-06 | P1 | Architecture | Provider list hardcoded in Rust state, commands, and UI; adding a provider touches 4+ places. | `main.rs` `AppState`, `get_codex/get_cursor/...`, `notch.html` `providers()` | `providers.rs`: `Provider` trait + `REGISTRY`; per-provider snapshot slots (own lock each); one `providers` event / `get_providers` command carrying `Vec<ProviderSnapshot>` with capabilities; UI renders the list; 4 per-provider commands/events removed | 7 unit tests in `providers.rs` (ids, order, visibility, failed providers listed, activity-only tail); verified in the installed app (pushed provider appears and leaves) | FIXED |
| W-07 | P1 | Providers | Windows has usage for 4 providers; macOS has ~12 (GLM, Ollama, Grok, OpenCode, Command Code, Copilot, Gemini API, Perplexity…). | `Sources/Providers` vs `windows/codenotch/src` | One adapter per commit on the W-06 registry (`providers::Simple` + shared `remote.rs`): Gemini API (local logs), Ollama local (`/api/ps`), Ollama Cloud (own key), GLM, OpenCode Go, GitHub Copilot, Grok, Command Code (borrowed keys; unofficial/private endpoints approved by the owner), Perplexity (in-app WebView, tray → Connect) | Fixture tests per adapter (35 new); Ollama local checked against a fake server, Perplexity checked against the live site (anonymous session → counts shown). Copilot/Grok/GLM/OpenCode/Command Code/Ollama Cloud not exercised live: no credentials on this machine | FIXED — live check per provider needs an account |
| W-08 | P1 | Activity | No per-provider activity capability. Users can't tell "idle" from "can't detect". | `activity.rs` | `Capabilities.activity` = `event` / `inferred` / `not_supported` per provider (Claude: event only with hooks installed; any provider with pushed rows: event; probed: inferred). The card shows the source on every provider; the list is republished after hook install/uninstall | 4 unit tests in `providers.rs`; checked in the installed app (Claude and Codex show "estimated" on a machine without hooks) | FIXED |
| W-09 | P1 | Install | v0.3.0 shipped as a zip with a bare, unsigned exe: no Start Menu, no uninstall, SmartScreen warning. | Download page | NSIS pipeline added (W-01/W-02). Code signing still needed. | — | PARTIAL |
| W-10 | P1 | Updates | No auto-update on Windows. | No updater plugin | `tauri-plugin-updater` (`updates.rs`): checks `releases/latest/download/latest.json` every 6 h (settings toggle), verifies the minisign signature against the shipped public key, offers "Install update" in tray + settings; nothing installs without a click. Key pair generated on the dev PC (password-protected, outside the repo); CI signs `win-v*` tags with the `TAURI_SIGNING_PRIVATE_KEY` secret and publishes `latest.json` + `.sig`; the local build script signs when the key file exists | 2 unit tests (shipped pubkey is a valid minisign key; feed is the owner's HTTPS releases); local signed build produced `.exe.sig`; installed app ran a check and reported "Could not fetch a valid release JSON" (no release yet) | FIXED — needs you to add the GitHub secrets, then a first `win-v*` release to test end to end |
| W-11 | P1 | UX | No onboarding or settings window on Windows; config is tray menu + `config.json`. | `tray.rs`, `config.rs` | Settings window (tray → Settings…): providers on/off and order (hidden cells leave the notch), Ollama Cloud key stored in Credential Manager, Perplexity connect/disconnect, Claude Code hooks, language, hover-only, start with Windows. First launch (no config.json) opens it with a welcome | 5 unit tests (`settings.rs` rows/order, `providers::arrange`) + a real Credential Manager round trip; checked in the installed app: first run shows the welcome, disabling Codex removed its cell, the order was saved | FIXED |
| W-12 | P1 | Freshness | Staleness judged in the UI with fixed windows (5 / 35 min); no offline vs error vs stale distinction; no sleep/wake refresh. | `notch.html` `freshWindow`, `staleOf` | `providers::freshness` per provider: live / stale / offline / error / needs_auth / no_data, with a per-source fresh window (API 11 min, Claude Desktop 35 min). Network failures set `UsageSnapshot.offline` (Claude, Codex, Cursor). Windows whose reset passed after the reading are marked `expired` and shown as "Window reset — waiting for a new reading" instead of an old %. A clock thread republishes every 30 s and refreshes all providers after a sleep/resume gap | 5 unit tests in `providers.rs`; checked in the installed app (a 158-day-old Codex reading now shows "—" and "Updated 158d ago"). Resume path covered by a unit test only | FIXED |
| W-13 | P2 | i18n | Card strings ("Resets in", "Usage", "Sign in to…") hardcoded English in `notch.html`; only the tray uses `i18n.rs`. | `notch.html` | Move to the page dictionary | — | OPEN |
| W-14 | P2 | Tests | No tests for usage parsing in `usage.rs`, `codex.rs`, `cursor.rs`, `antigravity.rs`. | `grep '#[test]'` | Fixtures in `codenotch/fixtures/` (see its README): one recorded + sanitized Codex rollout; the Claude, Codex usage, Cursor and Antigravity replies are hand-built from the parser shapes until sanitized recordings exist | 15 parser tests across `usage.rs`, `codex.rs`, `cursor.rs`, `antigravity.rs` (dedupe, clamping, dropped windows, labels, unlimited plans, backoff, token fingerprint) | FIXED (recorded fixtures for Claude/Cursor/Antigravity still wanted) |
| W-15 | P2 | Identity | Package metadata pointed to `Im-Midi/codenotch-windows`, identifier `com.immidi.codenotch`. | `Cargo.toml`, `tauri.conf.json` | Rebranded to `com.hegazy143585.codenotch` + this repo; upstream credits and MIT notices kept. User data stays in `%APPDATA%\codenotch`. Start-at-sign-in is re-pointed to the installed copy at launch so an old unzipped v0.3.0 can't grab the event port. | 3 unit tests in `autostart.rs` | FIXED — confirm on Windows |
| W-20 | P1 | Upgrade | Old zip users and installer users: two copies with different app ids can run at once and fight over port 48666. | `server.rs` bind error only logged to stderr | Bind failure now logged to run.log and shown as a sticky banner; the holder is probed (`GET /activity`) to say "Another Codenotch is running — close it" vs "port used by another program"; bind retried every 30 s and the banner clears once the port is free | 2 unit tests in `server.rs`; verified on Windows with a fake holder on 48666 (banner shown, then cleared on release) | FIXED |
| W-16 | P2 | Repo | 9 MB `site/Codenotch.dmg` committed to the repo. | `site/` | Move binaries to GitHub Releases | — | OPEN |
| W-17 | P2 | CI | `cargo audit` runs report-only. | `windows.yml` | Triage findings, then make it blocking | — | OPEN |
| W-18 | P3 | Security | `csp: null` in `tauri.conf.json`. | `tauri.conf.json` | Set a CSP (inline script/style + `data:` images); verify on Windows | Manual | OPEN |
| W-19 | — | iOS | No iOS code exists. | — | Deferred by decision: Windows only for now. Revisit sync model (local-only vs relay) when iOS is back in scope. | — | DEFERRED |

## Good as-is
- Loopback-only listener; Antigravity's self-signed TLS exception is pinned to `127.0.0.1`.
- Cursor's 2 GB `state.vscdb` is read through an mtime-gated cached connection.
- Claude API 429 back-off with a persisted deadline.
- No secrets found in the repo (pattern scan).
